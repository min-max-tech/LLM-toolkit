# SOPS configuration. Files matched by path_regex are encrypted to the
# age recipients listed here. The age public key is safe to commit
# (it can only encrypt; only the matching private key can decrypt).
creation_rules:
  - path_regex: secrets/.*\.sops$
    age: age1egt7028wtwpf3g9fqe5xvf80ptmhze25tltw0ff6nwv5hf6v6grqpl2qar
