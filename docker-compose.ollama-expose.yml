# Optional: expose Ollama on host port 11434 (for Cursor, direct CLI, etc.).
# Use: docker compose -f docker-compose.yml -f docker-compose.ollama-expose.yml up -d
# Default stack keeps Ollama backend-only (no host port).

services:
  ollama:
    ports:
      - "11434:11434"
