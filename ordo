#!/usr/bin/env bash
# Short launcher for Ordo
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ $# -eq 0 ]]; then
  exec "$SCRIPT_DIR/ordo-ai-stack" launch
else
  exec "$SCRIPT_DIR/ordo-ai-stack" "$@"
fi
