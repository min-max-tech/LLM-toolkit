# MCP Gateway secrets — copy to mcp/.env and add your keys.
# Required by some MCP servers (github-official, brave, etc.).
# Never commit mcp/.env

# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_...
# BRAVE_API_KEY=...
# DATABASE_URL=postgresql://user:pass@host:5432/db
