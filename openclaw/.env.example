# OpenClaw environment. Copy to .env and adjust.
# When run from openclaw/, BASE_PATH should point to repo root (e.g. F:/LLM-toolkit).

# Project root (use forward slashes)
BASE_PATH=F:/LLM-toolkit

# OpenClaw data paths (defaults: ${BASE_PATH}/data/openclaw and .../workspace)
# OPENCLAW_CONFIG_DIR=F:/LLM-toolkit/data/openclaw
# OPENCLAW_WORKSPACE_DIR=F:/LLM-toolkit/data/openclaw/workspace

# Gateway auth - set a long random token (required)
# Generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token

# Ports (defaults: 18789 gateway, 18790 bridge)
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_BRIDGE_PORT=18790

# Bind: loopback (127.0.0.1 only), lan (all interfaces), or a specific IP
# OPENCLAW_GATEWAY_BIND=lan

# -----------------------------------------------------------------------------
# Model provider API keys (set at least one for the agent)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# -----------------------------------------------------------------------------
# Optional: use custom image (e.g. build from openclaw repo)
# -----------------------------------------------------------------------------
# OPENCLAW_IMAGE=openclaw:local
