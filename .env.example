# Copy to .env and adjust.

# Project root - ALL data (Ollama, Open WebUI, ComfyUI, N8N) lives under this path.
# Use forward slashes. Example: D:/local-llm-docker
BASE_PATH=D:/local-llm-docker

# Optional: override data dir (default: BASE_PATH/data)
# DATA_PATH=D:/local-llm-docker/data

# Ollama models (comma-separated, no spaces)
MODELS=deepseek-r1:7b,deepseek-coder:6.7b,nomic-embed-text
