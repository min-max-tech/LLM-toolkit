# Copy to .env and adjust.

# Project root. All data lives under this path (no Docker volumes on C:).
# Use forward slashes.
BASE_PATH=F:/LLM-toolkit

# Optional: put data on a different path than BASE_PATH/data
# DATA_PATH=F:/LLM-toolkit/data

# Ollama models to pull on first start (comma-separated, no spaces)
MODELS=deepseek-r1:7b,deepseek-coder:6.7b,nomic-embed-text

# MCP Gateway — servers to enable (comma-separated, from Docker MCP Catalog)
# Examples: duckduckgo, fetch, dockerhub, github-official
# MCP_GATEWAY_SERVERS=duckduckgo
# MCP_GATEWAY_PORT=8811
